# App Store screenshots — 1.1

Six frames at 1320×2868 (6.9"), the 1.0.7 layout over fresh captures of build 37
(the restructured front end: three state cards on Home, four tabs, the IQ pill
on the Tactics rail). Same copy and same claim rules as 1.0.7 — see that
folder's README; nothing about ratings, download counts or roster size.
Regenerate: `python3 build.py && npx --yes hyperframes@0.8.20 snapshot --at 0.5,1.5,2.5,3.5,4.5,5.5`, then copy `snapshots/frame-0N` → `out/` in order.
